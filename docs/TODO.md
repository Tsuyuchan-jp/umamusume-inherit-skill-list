# TODO

着手・完了のたびに更新する。一度に全部やらない。

## 次（合意済み）

なし。v1 は公開済み。機能追加・見た目の作り直しはしない。

## 完了

- [x] **GitHub Pages 初回公開** — https://Tsuyuchan-jp.github.io/umamusume-inherit-skill-list/app/ 。`origin` は `Tsuyuchan-jp/umamusume-inherit-skill-list`。Actions でリポジトリ直下を配信
- [x] **CSS 間引き** — `chrome.css`＋`inherit.css`。sp-calc の `style.css` / `foundation.css` は削除
- [x] **公開前レビュー** — README 分離、非公式注記、Pages 入口、モック削除、未使用 JS/画像、テスト
- [x] **v1 機能＋デザイン** — 実機OK。これ以上の機能追加・見た目の大きな変更はしない
- [x] **結果枠** — 見出しは「不足しているかもしれないスキルリスト」。コピーは金ボタン「スキルリストをコピー」（幅100%・上限24rem）。ウマ娘DBは白枠の補助ボタン
- [x] U-tools リンク文言に場・距離（回り）・芝/ダ・脚質を入れる
- [x] **使い方ダイアログ** — 目的（ウマ娘DBでレンタル継承親を探すときの OR 元リスト）＋白∩共通から本育成分を除いた先頭25件。手順は常時表示。前提・見方は折りたたみ。extract は README のみ。レンタル因子貼り付けへリンク。外側クリックで閉じる
- [x] **背景＋ヘッダー** — UmaTools寄せ。暗い帽子＋白い机＋メッシュ下地（濃さ調整済み・実機OK）。全面イラスト廃止。リード文はヘッダーから外し、使い方へ移した
- [x] 使うコースの `extract:effects`（U-tools 金ドット36件。`--from-tracks`。140×4 の一括はしない）
- [x] コース選択は既定データありのみ。「すべてのコースを見る」で全件。データなしは薄く、金ドットは全件表示時のみ
- [x] コース選択 UI（場→距離チップ。セレクト廃止）
- [x] ダートコース抽出（U-tools は「ダ」。140件 = 芝80 + ダート60）
- [x] **スキルの手動除外** — 先頭25件の行から外す。下に除外中（戻す・有効順）。コピーは残件の先頭25件。コース＋脚質ごとに localStorage
- [x] 行タップ詳細（順位ドットはチャンミ/リグヒ切替。閉じた行は条件があるときだけ前/中後。数値列は詰める）

## 後回しでよい

- コースチップの視認性（**現状で足りる**。「すべて見る」は常用しないので今のまま）
- 全コース分の `extract:effects` 一括
- アプリ内から extract（いまは手動コマンド）
- フィルタ近似の精密化（手動除外で足りる想定。突き合わせは実機OK）

## やらない

- umamusume-sp-calc への機能追加
- ウマ娘DBの因子入力をこのアプリ単体でやる（rental-factor-fill を使う）
- 起動のたびに U-tools へフェッチ
- コース選択 UI の大きな作り直し（v1 では現状のまま進める）
- v1 完了後の見た目の作り直し
